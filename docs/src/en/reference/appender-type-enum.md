# AppenderTypeEnum

Appender type enum, used to find or remove appenders by type.

## Enum values

- `FILE` - File appender
- `CONSOLE` - Console appender
- `SOCKET` - Socket appender (TCP/UDP/SMTP)
- `DATABASE` - Database appender
- `CUSTOM` - Custom appender
